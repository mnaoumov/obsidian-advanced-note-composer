<!-- markdownlint-disable MD025 MD041 -- The two top-level headings ARE the fixture: each becomes a numbered folder, so neither may be a document title. -->
Split this note recursively to watch the numbering continue. Every heading below becomes a folder, and the
two top-level ones join the `1.` / `3.` / `4.` sequence already in this folder.

# D

The first new top-level heading.

## DD

Its first child. Inside the brand-new `5. D/` nothing is numbered yet, so it starts again at `1.`.

## DD2

Its second child.

# F

The second new top-level heading. By now `5. D` exists, so this one becomes `6.`.

## FF

## FF2
