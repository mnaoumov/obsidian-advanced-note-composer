# Inside

This note lives in `Movable folder`, which carries no number at all. Run `Move folder to...` and pick `Destination` - the folder arrives as `4. Movable folder`, and this note travels inside it.
