# Note one

This is the first note in `Flatten example`. It links to [Note two](<./Note two.md>).

After you run `Flatten folder...`, this note moves up into the vault root, and the link above still
resolves.
