# Placed note nearby

This note exists to be **picked, not written into**. It sits in a folder of its own so that picking it in the split/extract picker has somewhere distinct to put the new note.

See [15 Name first, then the folder](<../../03 Split/15 Name first, then the folder.md>).
