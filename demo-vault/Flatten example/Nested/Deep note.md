This note lives in the `Nested` subfolder. When you flatten `Flatten example`, the whole `Nested`
folder is promoted as-is, so this note stays inside `Nested` - just one level higher.

`Nested` has a `Deeper` subfolder of its own, holding [[Deepest note]] - which is what makes
`Flatten folder recursively (all folders at any depth)...` visible.
