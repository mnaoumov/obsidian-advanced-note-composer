# Already here

This note already lives in `Move destination`. It is here so the destination folder exists as a
target for the `Move folder to...` picker in [18 Move folder to](<../18 Move folder to.md>).
