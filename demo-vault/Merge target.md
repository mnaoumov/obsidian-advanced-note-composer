---
title: Project overview
tags:
  - project
---

This note is the **merge target** used by [[01 Merge file]].

It already has some frontmatter (`title`, `tags`) and its own content, so after the
merge you can see how Advanced Note Composer combines the two notes' frontmatter and
appends the source note's body here.
